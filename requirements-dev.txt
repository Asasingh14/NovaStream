# Development requirements (includes base requirements)
-r requirements.txt

# Testing
pytest
pytest-cov
coverage
bandit
safety
ruff

# Utilities
pipreqs 
authlib>=1.6.5 # not directly required, pinned by Snyk to avoid a vulnerability