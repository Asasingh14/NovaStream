# Development requirements (includes base requirements)
-r requirements.txt

# Testing
pytest
pytest-cov
coverage
bandit
safety
ruff

# Utilities
pipreqs 
authlib>=1.6.6 # not directly required, pinned by Snyk to avoid a vulnerability
filelock>=3.20.3 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability